export PRINTER='Lab'

USRLOCID=${GREEN}
TIMESTMP=${BROWN}
PATHLINE=${LIGHTBLUE}

if [ $1 = "hydro" ]; then
    source /opt/ros/hydro/setup.bash
elif [ $1 = "indigo" ]; then
    source /home/dan/ros_catkin_ws/install_isolated/setup.bash
fi

export GPG_TTY='tty'

echo "Welcome to Replicant!"
